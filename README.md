### SCSS compiler using Gulp


- install `nodejs` 
- install `npm` - check if this works `npm -v`


### Project setup
- install npm packages `npm i`
- run commands for the scss compiler `npm run dev` ili `npm run watch`
- only work with the `sass/main.scss`